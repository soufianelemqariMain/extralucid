[build]
dockerfilePath = "Dockerfile.railway"

[deploy]
preDeployCommand = "venv/bin/python manage.py migrate"
healthcheckPath = "/api/v1/questions/"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
